{
  "name": "physioez-app",
  "private": true,
  "version": "0.6.6.4-alpha",
  "type": "module",
  "scripts": {
    "tauri": "tauri",
    "start": "node server/src/server.js",
    "build": "npm install --prefix frontend && npm install --prefix server && npm run build --prefix frontend",
    "dev": "concurrently \"npm run dev --prefix frontend\" \"npm run dev --prefix server\""
  },
  "devDependencies": {
    "@tauri-apps/cli": "^2",
    "concurrently": "^9.2.1"
  },
  "dependencies": {
    "@tauri-apps/plugin-notification": "^2.3.3",
    "@tauri-apps/plugin-store": "^2.4.1"
  }
}
